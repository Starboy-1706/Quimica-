import type { NextConfig } from "next";

/**
 * Todas las imágenes editoriales se importan desde el panel y se sirven
 * por /imagenes/[key]. No se permiten hosts de imágenes remotos.
 */
const nextConfig: NextConfig = {};

export default nextConfig;
